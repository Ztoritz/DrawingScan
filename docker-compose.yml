version: '3.8'

services:
  backend:
    build: ./backend
    expose:
      - "8000"
    restart: always

  frontend:
    build: ./frontend
    expose:
      - "80"
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://localhost:8000}
    restart: always
    depends_on:
      - backend
